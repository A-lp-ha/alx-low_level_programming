#include "main.h"
#include <stdio.h>
/**
 * main - print name
 * @argc: number of command
 * @argv: array name
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
