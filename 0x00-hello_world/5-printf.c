#include <stdio.h>
/**
* main - Entry point
*
* return: Always end 0 (success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return: Always end 0 (success);
}
