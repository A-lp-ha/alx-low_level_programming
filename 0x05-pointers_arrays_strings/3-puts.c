#include "main.h"
#include <stdio.h>
#include <unistd.h>
/**
 * main - entry point
 *
 * Return: always 0
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	puts(str);
	return (1);
}
