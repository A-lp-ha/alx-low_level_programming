#include <unistd.h>
#include "main.h"
/**
 * main - entry point
 *
 * Return: always 0.
 */
int print_last_digit(int)
{
int digit= (x % 10);
if (n>0)
{
putchar(last+'0');
putchar('\n');
n++;
}
return (0);
}
	
