#include <stdio.h>
#include"main.h"
/**
 * add -entry point
 * @a: integer first input
 * @b: integer second input
 * description: return sum of two integers
 * Return: always 0.
 */
int add(int a, int b)
{
	int results = a + b;

	return (results);
}
