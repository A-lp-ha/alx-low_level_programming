#include <unistd.h>
#include "main.h"
#include <stdio.h>
/**
 * main - entry point 
 *
 * Return: always 0.
 */
int _abs(int)
{
int x;
int r = _abs x;
for(x <= 0; x <= 10; x++)
{
purchar('r');
}
return (0);
}

