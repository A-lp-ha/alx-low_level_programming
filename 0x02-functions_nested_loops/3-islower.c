#include "main.h"
#include <unistd.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _islower(int c)
{
int y = 'a'
for(y = 'a'; y <= 'z'; y++)
{
if ( c == y)
{
return (1);
}
else if
{
return (0);
}
}
return (0);
}
