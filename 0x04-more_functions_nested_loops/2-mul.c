#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * mul - check code
 * @a: variable
 * @b: variable
 *
 * Return: always 0.
 */

int mul(int a, int b)
{
int sum;

sum = a * b;
return (sum);
}
